---
layout: post
title: Aurora bug: a point for open-source
tags: devops postgresql
category: 
---

time to upgrade postgres
if we can do Mysql -> postgres, then we can do postgres -> postgres
actually, we have aurora with one master and one replica

try in staging:
postgres standard logical replication v13 -> v14
let the replication run for a few days
Ok

try in prod:
postgres standard logical replication v13 -> v14
let the replication run for a few days
API shutdown. hundreds of bugs show up in slack

replica target machine was undersized. if replica target crashes, than replica source target also!?
